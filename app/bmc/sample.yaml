sample:
  name: JTAG
tests:
  sample.app:
    sysbuild: true
    platform_allow:
      - tt_blackhole/tt_blackhole/bmc
    harness: console
    harness_config:
      type: one_line
      regex:
        - "BMFW VERSION (.*)"
