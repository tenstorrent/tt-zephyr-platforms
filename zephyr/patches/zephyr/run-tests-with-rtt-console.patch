diff --git a/samples/hello_world/sample.yaml b/samples/hello_world/sample.yaml
index 1bcb7db62f2..986f3235e5f 100644
--- a/samples/hello_world/sample.yaml
+++ b/samples/hello_world/sample.yaml
@@ -6,11 +6,16 @@ common:
   tags: introduction
   integration_platforms:
     - native_sim
   harness: console
   harness_config:
     type: one_line
     regex:
       - "Hello World! (.*)"
 tests:
-  sample.basic.helloworld:
-    tags: introduction
+  sample.basic.helloworld: {}
+  sample.basic.helloworld.rtt:
+    filter: CONFIG_HAS_SEGGER_RTT
+    required_snippets:
+      - rtt-console
+    tags:
+      - rtt
diff --git a/tests/boot/test_mcuboot/testcase.yaml b/tests/boot/test_mcuboot/testcase.yaml
index ffa090f882b..5459833a66f 100644
--- a/tests/boot/test_mcuboot/testcase.yaml
+++ b/tests/boot/test_mcuboot/testcase.yaml
@@ -3,12 +3,12 @@ common:
   tags: mcuboot
   harness: console
   harness_config:
-    type: multi_line
+    type: one_line
     regex:
-      - "I: Starting bootloader"
-      - "Launching primary slot application on (.*)"
-      - "Secondary application ready for swap, rebooting"
-      - "I: Starting swap using (.*)|I: Image 0 upgrade secondary slot -> primary slot"
+#      - "I: Starting bootloader"
+#      - "Launching primary slot application on (.*)"
+#      - "Secondary application ready for swap, rebooting"
+#      - "I: Starting swap using (.*)|I: Image 0 upgrade secondary slot -> primary slot"
       - "Swapped application booted on (.*)"
 tests:
   bootloader.mcuboot:
@@ -62,3 +62,9 @@ tests:
     extra_args:
       - SB_CONFIG_MCUBOOT_MODE_SWAP_USING_OFFSET=y
       - swapped_app_CONFIG_MCUBOOT_BOOTLOADER_MODE_SWAP_USING_OFFSET=y
+  bootloader.mcuboot.rtt:
+    tags:
+      - rtt
+    filter: CONFIG_HAS_SEGGER_RTT
+    required_snippets:
+      - rtt-console
