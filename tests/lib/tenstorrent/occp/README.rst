This test validates that the OCCP subsystem is functional. It
expects to be run in an SIP testbench, with the remote SMC running
the production ROM as an OCCP target.

The test will verify that the OCCP version matches what is expected,
and then will read/write from the SMC's memory.
