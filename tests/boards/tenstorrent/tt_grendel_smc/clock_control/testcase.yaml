common:
  tags:
    - smoke
    - drivers
    - clock_control
tests:
  drivers.clock_control.grendel_smc.basic:
    platform_allow:
      - tt_mimir/tt_mimir/smc
    extra_args:
      - "platform:tt_mimir/tt_mimir/smc:DTC_OVERLAY_FILE=app.overlay"
